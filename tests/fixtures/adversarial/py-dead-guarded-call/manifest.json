{
  "case_id": "py-dead-guarded-call",
  "language": "python",
  "family": "dead_code",
  "family_tag": "fp_dead_code",
  "description": "Side B references the deleted export only inside an `if False:` block (statically unreachable). A resolver that flags refs without reachability awareness predicts a break here.",
  "predictions": [
    {
      "prediction_id": "py-dead-guarded-call-p0",
      "conflict_class": "removed_export",
      "symbol_id": "pkg.models::format_greeting",
      "ref_path": "pkg/app.py",
      "anchor": "pkg.models.format_greeting(user)",
      "resolution_status": "resolved"
    }
  ],
  "expectation": {"py-dead-guarded-call-p0": "false_positive"},
  "notes": "FP trap #4: expectation encodes default mypy behavior for statically unreachable code; verified empirically when this fixture was authored."
}
